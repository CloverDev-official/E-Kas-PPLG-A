<header class="border-b-2 border-divider pb-5" >
    <h1 class="text-2xl font-semibold capitalize">{{ str_replace( '-', ' ', Route::currentRouteName() )  }}</h1>
    <p class="text-mutedForeground font-reguler">{{ $tanggal }}</p>
</header>